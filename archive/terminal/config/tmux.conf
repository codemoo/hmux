# Retired compatibility marker.
#
# hmux 0.1.13 and later never source this file into the user's tmux server.
# All hmux UI, tabs, aliases and shortcuts live outside the target server in
# config/frame.tmux.conf.
