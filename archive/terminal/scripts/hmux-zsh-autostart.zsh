# Retired compatibility marker. Old shell includes may still source this file.
# Opening a shell must not install or launch HMux. Use the app or an explicit
# repository entrypoint; sync-ui-config-macos.sh removes recognized old includes.
