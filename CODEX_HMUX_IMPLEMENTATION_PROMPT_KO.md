# HMux 제품·보안 명세

이 문서는 현행 제품 방향과 구현 제약이다. 실제 코드와 검증 결과를 함께 확인하며,
문서 자체를 배포나 실서버 변경 승인으로 취급하지 않는다.

## 목적과 지원 범위

HMux는 하나의 Home 호스트에서 Codex, Claude Code, 일반 셸을 계속 실행하고
외부 어디에서나 웹·PWA로 이어 사용하는 셀프호스팅 도구다. 사용자 인터페이스는
웹·PWA만 제공한다. 별도 터미널 선택기·프레임 UI와 macOS 앱 및 전용 배포 경로는
유지하지 않는다. 웹 터미널 자체와 tmux·셸·제공자 CLI 실행 기능은 핵심 기능이다.

현재 검증된 배포는 macOS Home과 Linux HTTPS gateway 조합이다. macOS 호스트
지원과 macOS 앱 지원을 혼동하지 않는다. 공개 저장소는 독립 배포용 소스이며
운영자의 호스트나 계정 접근을 제공하지 않는다. 계정들은 같은 Home의 셸 권한과
파일을 공유하는 신뢰 관계이며, 불특정 사용자를 격리하는 멀티테넌트 서비스가 아니다.

## 구성과 책임

- `web/`: TypeScript·xterm.js 기반 데스크톱·모바일 웹 및 PWA.
- `cmd/hmux-web`, `internal/webgateway`: 인증·프로필·WebSocket gateway와 Home connector.
- `internal/home`: 웹 connector가 직접 호출하는 로컬 tmux 뷰와 호스트 작업.
- `internal/agent`, `internal/catalog`: 프로필 실행, 세션 메타데이터, 정확한 제공자 연결.
- `internal/recovery`, `internal/sharedworkspace`: 검증된 복원과 탭 상태.
- `cmd/hmux-agent`: 관리·복구·진단·워크플로 훅용 명령. 별도 사용자 UI가 아니다.
- `deploy/web`: 웹 자산, Linux gateway, macOS Home 바이너리 빌드와 배포 템플릿.

브라우저는 HTTPS/WSS로 gateway에, Home은 인증된 outbound WSS로 gateway에 연결한다.
Home에 인바운드 포트를 열거나 원격 장치의 SSH 클라이언트를 구성할 필요가 없다.
Gateway는 인증과 TLS를 종료하므로 터미널 입력 권한을 가진 신뢰 주체다.
Home connector는 로컬 서비스를 직접 사용하며 앱 브리지나 SSH 프로세스를 거치지 않는다.

## 세션과 연결

모든 세션 작업은 `{id, created_at}`로 같은 tmux 수명인지 확인한다. 이름만으로 연결하거나
재사용된 ID를 예전 세션으로 취급하지 않는다. 생성은 호스트 inventory의 허용된 프로필
인자 배열을 사용하며, 생성 결과의 실제 ID와 생성 시각을 반환한다.
설치 시 새 세션의 기본 경로를 선택하며 신규 설치 기본값은 `~/.hmux`다. 기존 설정의
경로는 유지한다. 세션마다 입력한 이름을 안전한 폴더명으로 변환해 기본 경로 아래에
새 하위 폴더를 만들고, 같은 이름의 폴더나 tmux 세션을 재사용하지 않는다. tmux 이름에는
실제 하위 폴더명·프로필·충돌 방지 접미사를 넣는다. Codex·Claude 실행 및 복원은 CLI가
끝나도 같은 작업 디렉터리의 대화형 셸을 남긴다. 기존 실행 중인 pane은 변경하지 않는다.

브라우저 터미널은 원본 창을 공유하는 임시 grouped tmux 뷰를 갖는다. 크기 변경·다시그리기·
접속 해제는 해당 뷰에만 적용하고 원본 세션과 제공자 프로세스를 종료하지 않는다.
기존 웹 connector와의 순차 업그레이드를 위해 `@hmux_app_view`와 `hmux-app-view-`
내부 표식은 보존한다. 이것이 별도 앱 지원을 의미하지 않는다.

브라우저당 보이는 탭 하나만 연결하고 gateway 전체 뷰 수·요청 수·버퍼·프레임·시간을 제한한다.
출력은 브라우저 소비량에 맞춰 흐름을 제어한다. 연결 세대가 지난 콜백은 폐기하며, 느린 출력이나
재연결이 무제한 큐·중복 연결을 만들지 않아야 한다. catalog와 사용량은 공용 수집기를 사용한다.

같은 계정의 탭 추가·닫기·순서는 공유하고 선택 탭·테마·폰트는 기기별로 유지한다.
탭 닫기는 목록 변경이며 작업 종료가 아니다. 기본 계정은 Home 탭 저장소를 쓰고 추가 계정은
gateway의 사용자별 프로필 저장소를 쓴다. 인증된 서버 계정만 프로필을 결정한다.

## 계정 보안과 알림

비밀번호·선택적 TOTP 로그인, Secure/HttpOnly/SameSite=Strict 쿠키, 정확한 Origin·Host,
CSRF, 로그인 시도 제한을 유지한다. 공개 회원가입이나 임의 명령 실행 API는 제공하지 않는다.
TOTP는 기본 활성화하며 현재 비밀번호와 미사용 인증 코드로 계정별 on/off를 변경한다.
변경 브라우저의 로그인은 원래 만료를 유지하고 해당 계정의 다른 로그인만 폐기한다.

로그인은 최대 7일의 절대 만료를 가지며 서버 재시작 후에도 유지한다. 계정별 세션 목록은
브라우저·IP·추정 위치·시각을 보여주고 자기 계정 로그인만 폐기한다. 토큰 원문은 저장하지
않는다. 세션 폐기는 저장 후 성공 처리하고 관련 연결을 닫는다. 저장 오류는 인증을 거부한다.
외부 IP 위치 조회는 승인된 추정 정보이며 자격 증명을 보내지 않는다.

완료 알림은 기기에서 명시적으로 켠 Web Push만 사용한다. Home의 정확히 연결된 Codex
작업 완료를 감지하여 해당 로그인 계정의 열린 탭에 보낸다. 탭 이름과 완료 상태만 표시하고
대화는 포함하지 않는다. 클릭 시 로그인 소유권과 세션 수명을 확인하여 해당 탭으로 이동한다.
로그아웃·폐기·만료 후 발송을 중단하며 푸시 키와 구독은 비공개로 보존한다.

프론트 진단은 계정별로 분리하고 고정된 오류 종류·상태·재연결·시간·브라우저/빌드 정보만
유한하게 수집한다. 입력·출력·비밀번호·토큰·원문 예외·스택·URL은 수집하지 않는다.
설정에서 내려받을 수 있고 진단 실패가 인증·연결 복구를 방해하거나 오류를 재귀 생성하지 않는다.

## 웹 입력과 화면

모든 입력 변경은 `web/AGENTS.md`, `docs/WEB.md`, `docs/IOS_INPUT.md`를 따른다.
현재 사용자가 수용한 iPhone·macOS Safari 한글 입력, 경계 전송, 기본 붙여넣기 동작을 보존한다.
Android는 기본 xterm 입력을 사용한다. 브라우저가 조합·삭제를 소유하며 별도 한글 재조립,
원격 삭제 후 치환, 독립된 입력창을 도입하지 않는다. 표준 composition은 xterm이 처리한다.
추가 우회 처리는 실제 기기 이벤트 근거가 있어야 하며 자동 테스트와 기기 검증을 구분한다.

작성 중 미리보기와 커서는 터미널 화면 안에 제한하고 현재 터미널 배경·폰트를 반영한다.
모바일 키보드·safe-area는 viewport 코드에서 관리한다. 보조키는 문자가 잘리지 않고
입력 포커스를 유지해야 한다. 출력 선택 시 iPhone 키보드가 내려가는 수용된 제약을 보존한다.
데스크톱 드래그 복사와 브라우저 기본 선택 메뉴를 유지한다. URL은 명시적 열기 버튼을
통해 새 창에서 열고 HTTP(S) 검사와 noopener/noreferrer를 적용한다.

색상·터미널 테마·폰트는 검증된 기기별 설정을 사용한다. 테마 변경이 연결을 재시작하지
않아야 한다. 웹 폰트의 한글 범위와 Regular/Bold, WOFF2/TTF 및 라이선스를 유지한다.
외부 값은 안전한 DOM으로 렌더링하며 worker에 자격 증명·대화·터미널 내용을 저장하지 않는다.

## 대화·사용량·파일

대화는 요청할 때만 정확한 제공자 연결로 읽는다. 다른 프로젝트의 유사한 로그를 추측하지
않는다. 내부 채널·도구 기록·환경/지침 래퍼·압축 요약은 숨기고 사용자 메시지와 일반 응답은
보존한다. Markdown 표·코드·목록은 안전한 allowlist DOM으로 표시하고 원문 HTML은 실행하지 않는다.

사용량은 Home의 기존 인증을 사용한다. 설정에서 Codex CLI/codex-lb, Claude CLI/cswap을
각각 선택하고 표시를 끌 수 있다. Codex를 먼저 표시하고 요금제는 명시적 정보만 사용한다.
5시간 제한이 없는 활성 계정/모델이 하나라도 있으면 통합에도 그 제한을 표시하지 않는다.
주간 리셋까지의 시간을 표시하고 실패·오래된 값·미제공 값과 0 사용량을 구분한다.
cswap 계정 전환이나 별도 인증 갱신 데몬을 도입하지 않는다.

드래그/모바일 첨부는 크기·개수·저장량을 제한하고 원래 탭의 정확한 수명을 전후 확인한다.
파일 이름은 브라우저에만 유지하며 Home에는 임의의 비공개 이름으로 저장한다. 완료 후 경로만
인용하여 붙여넣고 Enter는 보내지 않는다. 탭·계정 전환으로 대상을 바꾸지 않는다. 업로드 파일은
완료 시점부터 3시간 후 만료하며 실패/취소 시 미완료 파일을 정리한다.

## 복원·설정·검증

Home의 checkpoint는 검증된 세션·레이아웃·작업 디렉터리·제공자 연결을 저장한다.
다음 부팅 연결 시 없는 세션만 복원하며 정확한 Codex/Claude 대화만 resume한다.
임의 셸 명령·프로세스 메모리·스크롤백은 복원하지 않는다. 이름 충돌은 덮어쓰지 않고
검증된 수명 매핑으로만 탭을 연결한다. checkpoint 실패가 임의 세션 종료로 이어지지 않는다.

새 설정은 `home.toml`과 프로필 inventory다. 기존 `client.toml`과 알려진 구형 항목은 읽기
호환만 제공하고 사용하지 않는다. 알 수 없는 항목은 거부한다. 실제 설정을 자동 덮어쓰지 않고
변경 시 타임스탬프 백업을 남긴다. 런타임 상태와 자격 증명은 릴리스·Git 밖에 보존한다.
SSH 관리 작업에서는 호스트 키 검증과 인자 검사를 유지하며 agent forwarding은 금지한다.
다른 앱의 비공개 저장소를 읽거나 편집하지 않는다. macOS LaunchAgent는 도입하지 않는다.

`make check`, `make integration`, `make build`로 Go·수집기·웹·셸·격리 tmux 수명주기를
검증한다. 테스트는 기존 tmux 세션에 attach·rename·detach·kill하지 않으며 전용 socket과
`hmux-e2e-*` 리소스만 쓴다. 실제 기기 확인, 자동 확인, 미검증 사항을 구분하여 기록한다.
배포는 기존 릴리스와 비공개 상태를 보존하고 웹·gateway·Home 범위를 명확히 보고한다.
