# HMux 제품·보안 명세

이 문서는 현행 제품 방향과 구현 제약이다. 예전 fzf 중심 구축 프롬프트를
대체한다. 실제 코드와 검증 결과를 함께 확인하며, 이 문서 자체를 배포나
실서버 변경 승인으로 취급하지 않는다.

## 제품 목적

HMux는 하나의 호스트에서 Codex, Claude Code, 일반 셸 작업을 계속 실행하고
외부 어디에서나 웹·PWA로 이어서 사용하는 셀프호스팅 도구다. 데스크톱·휴대폰·
태블릿은 같은 호스트의 tmux 작업에 연결한다. 현재 검증된 배포는 macOS 호스트와
Linux HTTPS gateway 조합이다. 공개 저장소는 각 사용자의 독립 배포를 위한 소스이며
운영자의 호스트나 계정 접근을 제공하지 않는다.

기본 제품은 웹·PWA다. Ghostty 기반 macOS 앱은 `macos/`의 별도 선택 클라이언트로
유지한다. 기존 fzf/framed 터미널 UI와 전용 설정·테스트는 `archive/terminal/`에
보관한다. 웹과 앱이 공유하는 SSH·tmux·세션 서비스 및 native Go bridge는 유지한다.
웹 계정은 신뢰하는 사용자에게 같은 호스트 실행 권한을 제공하며, 불특정 사용자
사이의 OS 격리나 멀티테넌트 서비스를 제공하지 않는다.

CLI/fzf는 관리·복구·모바일 호환 경로다. 웹 클라이언트는
Linux Nginx HTTPS 프록시와 비밀번호·TOTP 로그인을 사용한다. 공개 회원가입,
임의 원격 명령 API는 범위 밖이다. 소유자가 승인한 추가 웹 계정은 별도
비밀번호·TOTP와 탭 프로필을 가지며, tmux 작업과 실행 권한은 공유한다.
기본 계정의 네이티브·웹 탭 연결은 유지하고 추가 프로필은 gateway에 저장한다.
같은 계정의 탭 추가·닫기·순서는 모든 클라이언트에서 공유한다. 선택한 탭은
기기별로 유지한다. 연결 중에는 주기적으로 반영하고 백그라운드 복귀 시 갱신한다.
탭 닫기는 공유 목록 변경이며 원본 tmux 작업 종료가 아니다.

TOTP 로그인은 기본적으로 켜져 있으며, 계정 설정에서 사용자별로 켜고 끌 수 있다.
변경하려면 현재 비밀번호와 기존 인증 앱의 아직 사용하지 않은 코드를 확인한다.
끄면 비밀번호만으로 로그인하며, 다시 켜도 기존 인증 앱 등록을 그대로 사용한다.
변경한 브라우저의 로그인은 유지하고 해당 계정의 다른 로그인만 폐기한다.
설정은 비공개 타임스탬프 백업 후 저장하고 재시작 후에도 유지한다.

웹 로그인은 로그인 시점부터 최대 7일 유지하며 서버 재시작 후에도 보존한다.
설정의 로그인 세션 관리는 계정별로 격리하고 브라우저·접속 IP·추정 위치·로그인 및
활동 시각을 표시하며 해당 사용자 소유 세션만 폐기할 수 있다. 로그인 토큰 원문은
서버 파일에 저장하지 않는다. 위치는 사용자 승인된 외부 IP 조회의 추정값이다.

웹은 TypeScript·xterm.js와 단일 Go 서버로 가볍게 유지하며, 데스크톱·모바일
브라우저를 함께 지원한다. Home 연결은 outbound WSS connector로 제공한다.
Home의 공통 세션·복원·사용량 코드를 재사용하고, provider ID를 웹 탭에 저장하지
않는다. 웹 gateway는 인증과 TLS를 종료하므로 Home 터미널 입력 권한을 가진
신뢰 주체다. 이는 아래의 네이티브 SSH용 TCP relay와 다른 보안 경계다.
배포·제한·검증 기준은 `docs/WEB.md`를 따른다. 웹은 Android·iPhone의 PWA
설치를 지원한다. Android는 xterm 기본 입력을 유지한다. iOS는 실제 기기 기록으로
확인된 기본 입력의 한글 자모 조기 전송을 보정한다. 같은 네이티브 입력칸에서
브라우저가 조합·삭제하게 두고, 터미널 안에 작성 중인 내용을 보여주며 공백·Enter
등의 경계에서 한 번 전송한다. 별도 입력창·자체 한글 재조립·원격 삭제 후 치환을
도입하지 않는다. macOS Safari도 실제 Safari 18.6 기록에서 확인된
input-before-keydown229 및 insertReplacementText 경로에 같은 로컬 조합·미리보기·경계
전송 방식을 적용한다. 데스크톱 입력칸 배치와 마우스 선택은 유지하며, 현재 한글
입력은 2026-09-10 사용자가 수용했다. 표준 composition 이벤트는 xterm이 처리한다. 추가 보정에는
실제 기기 이벤트 근거를 요구하며 자동 테스트와 기기 수용 검증을 구분한다.
모바일 붙여넣기는 같은 입력칸의 OS 기본 메뉴를 사용한다. Android는 키보드가
열려 있고 입력칸이 포커스된 동안 현재 입력 줄을 터치 대상으로 제공하며, 키보드를
열 때의 왼쪽 위 기준점은 유지한다. iPhone에서 출력 선택 시 키보드가 내려가는
현행 제약은 사용자가 수용했다. 이를 키보드 유지 선택 기능의 구현 완료로
표현하거나 별도 요청 없이 다시 작업하지 않는다. 상세 동작은 `docs/WEB.md`가
관리한다. 브라우저당 현재 탭만 연결하고 비활성화·연결 종료가 원본 tmux 작업을
종료하지 않아야 한다. 웹 변경 시
`web/AGENTS.md`의 유지보수·검증 기준을 함께 따른다.

## 구성과 책임

| 구성 | 책임 |
| --- | --- |
| Home Mac | 장기 실행 tmux, hmux-agent, 메타데이터, provider 인증 원본 |
| 네이티브 앱 | Ghostty surface, 시각적 탭, 검색·상태·사용량 표시 |
| Go hmux | SSH/tmux 실행, 입력 검증, 앱 브리지, 업데이트, 관리 CLI |
| 외부 Mac | 로컬 UI와 자체 SSH 개인키 |
| DMZ Linux | 인벤토리·서명 릴리스, ProxyJump, systemd 사용자 타이머 |
| Termius | 지원되는 UI로 설정한 jump와 Home 모바일 선택기 |

Swift는 임의 SSH 문자열이나 tmux 명령을 만들지 않고 고정 Go 브리지를
호출한다. 대상은 표시 이름 대신 `{id, created_at}`로 전달한다.
Home이 세션과 표시 메타데이터의 원본이다.

DMZ는 TCP 점프 호스트다. 클라이언트의 한 OpenSSH 연결로 Home과 직접
인증하며 DMZ에 Home 개인키를 복사하거나 중첩 SSH를 기본 경로로 삼지 않는다.

## 데스크톱 동작

앱 전체는 Omarchy Flexoki Dark 팔레트를 쓰며 시스템의 Light 설정과 관계없이
Dark 모드를 유지한다. macOS의 기본 창·입력·키보드 조작은 유지한다.

- 별칭, 이름, 프로젝트 경로, 런타임, 모델, 태그 등 검증된 메타데이터로 검색한다.
- 목록은 별칭이 있으면 별칭, 없으면 세션 이름으로 A–Z 정렬한다. 상태 변화로
  행을 다른 섹션으로 이동시키지 않고 행 표시와 필터로 활동·주의·연결을 구분한다.
- 갱신이 스크롤, 클릭, 메뉴, 검색, 관리 입력을 방해하지 않아야 한다.
- 유효 스냅샷을 받기 전에는 Connected로 표시하지 않는다.
  재연결 중 마지막 정상 목록을 유지하되 제한 시간 이후 Offline으로 표시한다.
- 탭 닫기·앱 종료와 작업 종료를 구분한다. 실제 종료는 대상 확인과
  명시적인 사용자 확인이 필요하다.
- stable ID가 재사용되면 이전 탭이나 관리 요청이 새 세션에 동작하지 않는다.
- 단축키는 README와 실제 SwiftUI/Ghostty 설정에 맞춘다.
- 모든 네이티브·웹 탭과 순서는 공용이며 Home이 저장 원본이다. 한 기기의 탭
  닫기는 다른 기기에도 반영하지만 원본 tmux 작업은 종료하지 않는다. 선택한
  탭과 패널 표시는 기기별로 유지한다. 동시 변경은 전체 목록 덮어쓰기 대신
  변경분을 합치며, 불확실한 전송 실패만 동일 operation ID로 재시도한다.
  재부팅 후 Home이 검증한 이전·새 식별자 연결만 허용하고, 없어진 탭은
  복구될 때까지 유지한다. 이름이나 재사용된 숫자 ID로 연결하지 않는다.
- 연결 종료는 탭을 자동으로 없애지 않고 기존 화면과 재연결 동작을 남긴다.
- 탭 닫기는 최근 사용 탭으로 돌아가며, 명시적으로 닫은 탭은 현재 카탈로그에
  같은 세션이 남아 있을 때만 복구한다.
- 검색·관리창의 입력을 늦게 도착한 터미널 포커스 요청이 가로채지 않는다.

네이티브 탭은 원본 window를 공유하는 임시 grouped tmux view를 사용한다.
임시 view의 상태줄만 조정하고 원본 옵션·키 바인딩을 UI 저장소로 쓰지 않는다.
grouped view의 attach·크기·handoff 동작은 실제 tmux로 검증한다.
일반 CLI의 `attach-session -d`와 같은 효과라고 추정하지 않는다.

## 데이터와 에이전트 상태

표시 이름, 숨김 상태, 프로파일 정보, workflow 상태는 Home의 소유자 전용
파일에 저장하며 원자적으로 교체한다. 숨김은 실제 세션을 분리·종료하지 않는다.

카탈로그는 제한된 tmux/process 메타데이터만 반환한다. pane title이나 세션명만
보고 AI 실행을 확정하지 않는다. 알 수 없는 모델·진행 시간·상태를 만들어내지 않는다.

Codex workflow는 선택 기능이다. lifecycle ID는 일방향 해시로 저장하고
개수·문자열·보존 기간을 제한한다. hook 오류는 Codex 작업을 막거나 결과를
바꾸지 않는다. 설치·신뢰 검토는 `docs/CODEX_WORKFLOWS.md`를 따른다.

일반 카탈로그·workflow 경로는 prompt, response, transcript, pane 내용, 전체 argv, tool 입력·결과,
비밀번호·토큰·개인키를 로그나 workflow에 기록하지 않는다. 카탈로그의
프로젝트 경로도 민감할 수 있으므로 화면·진단 공유 시 보호한다.

## Home 재부팅 복원

소유자가 tmux 및 Codex·Claude 대화 세션 복원을 승인했다. Home의 foreground
카탈로그 연결 중 tmux 창·pane·작업 경로와 공통 resolver가 확인한 정확한
provider 대화 ID·설정 루트를 소유자 전용 상태에 원자적으로 저장한다. 대화
본문·전체 argv·임의 shell 명령·인증 정보는 저장하거나 재실행하지 않는다.
재부팅은 Home boot identity로 구별하며, 다음 HMux 연결에서 복원한다. 같은
부팅 중 의도적으로 없앤 세션은 자동 부활시키지 않는다. 기존 이름의 세션은
덮어쓰지 않으며, 복원은 새로 만든 세션에만 고정된 resume 명령으로 수행한다.
복원 시 tmux 서버도 자동 시작한다. 재부팅 후 저장된 체크포인트가 비어 있고
현재 세션도 없으면 Home 사용자 홈에 detached `hmux` 로그인 셸을 생성한다.
체크포인트 자체가 없는 최초 연결은 현재 상태만 저장한다.
LaunchAgent·상시 daemon은 추가하지 않는다. 일반 프로세스와 진행 중 도구
실행 상태의 메모리 복원은 제공하지 않는다.

## 명시적 Codex 대화 보기

소유자가 대화 보기 기능을 승인했다. 사이드바에 대화 목록을 추가하지 않고,
선택한 탭의 작업 영역에서 터미널과 읽기 보기를 전환한다. 기존 터미널은
유지하고 입력 포커스만 차단·복원한다. 기본은 답변만, 질문 포함 보기와
코드 블록 표시·숨기기, 검색·복사를 제공한다.

이 모드가 열려 있을 때만 Home의 해당 Codex 프로세스에 연결된 구조화된
기록에서 사용자 메시지와 사용자에게 공개된 assistant 메시지를 읽는다.
내부 추론, system/developer 지침, 도구 호출·출력은 전송하지 않는다.
파일 경로·provider thread ID를 클라이언트에서 지정할 수 없으며 tmux의
`{id, created_at}`를 읽기 전후 검증한다. 매칭이 모호하면 조회 불가를 표시한다.
본문은 제한된 메모리 응답으로만 전달하고 카탈로그·workflow·로그·디스크 캐시에
저장하지 않는다. 닫기·탭 변경 시 진행 중 조회를 취소하고 본문을 비운다.
SSH·소스 바인딩·파일 소유권·symlink·크기 검증은 유지한다.

## 연결·파일 전송·사용량

앱 소유 foreground helper가 카탈로그와 사용량을 전달한다. 로컬 WSS는
임시 loopback 주소, 정확한 인증서 pin, 일회용 bearer token을 사용한다.
원격 producer는 기존 SSH 신뢰 경계를 사용한다. 프레임 크기·순서·heartbeat
lease를 검증하고 앱 종료 시 helper를 회수한다.

사용량은 Home의 기존 provider 인증을 읽어 수집한다. 인증을 새로 만들거나
갱신하거나 다른 Mac으로 복사하지 않는다. 별도 고정 포트, 외부 usage
서비스, macOS daemon을 추가하지 않는다.
하단 사용량은 주간(1w) 잔여량이며, Codex는 설정된 codex-lb 계정 풀의
공식 잔여 비율을 우선한다. 소유자가 요청한 codex-lb 계정 별칭(alias)과
계정별 주간 잔여량·초기화 시각은 상세 화면에 전달한다. 별칭이 없으면
Account N으로 표시하고 이메일·displayName으로 대체하지 않는다. 별칭은 길이와 제어문자를
제한하고, 계정 ID·원본 인증·토큰을 추가로 노출하지 않는다.
Claude는 cswap의 활성 계정 주간 사용량을 대표로 표시하며, 상세에는 소유자가
명시적으로 요청한 계정별 이메일·활성 여부·사용량을 전달한다. 이메일도 길이와
제어문자를 제한한다. 활성 계정이 없거나 모호하면 다른 계정의 수치로 대체하지 않는다.

파일 drop은 일반 파일 1–16개, 파일당 32 MiB, 요청당 128 MiB로 제한한다.
로컬 절대경로와 원래 파일명은 Home 전송 데이터에 포함하지 않는다.
검증된 Home 경로만 POSIX quoting하여 입력하며 Enter를 보내지 않는다.
업로드 중 탭·surface·선택 epoch·활성 앱이 달라지면 원래 탭에 명시적인
붙여넣기 동작을 남긴다. 웹은 데스크톱 드롭과 모바일 파일 선택을 제공하고,
웹 첨부 임시 파일은 전송 완료 후 3시간에 만료한다. Home foreground connector가
시작 시 및 주기적으로 만료 파일을 삭제한다. Home 중단·절전 중 만료된 파일은
재개 후 정리하며 별도 daemon은 추가하지 않는다.

하단 Claude/Codex 사용량 옆에는 모든 tmux가 실행되는 단일 Home Mac의
CPU·GPU·RAM을 표시한다. 탭별로 수집하거나 원격 클라이언트의 로컬 자원을
대신 표시하지 않는다. 기존 foreground 카탈로그 연결당 한 수집 결과를
공유한다. 수집 실패·지원되지 않는 GPU·오래된 값은 0%로 만들지 않는다.
호환 에이전트에만 선택적 기능을 요청하며 별도 서비스·고정 포트를 추가하지 않는다.

## 설치·릴리스

`VERSION`은 소스 릴리스 버전이다. 이미 발행된 같은 버전의 아티팩트를
다른 내용으로 덮어쓰지 않는다. 로컬 빌드를 배포 성공으로 보고하지 않는다.

- 앱 설치는 `scripts/install-hmux-app.sh`, 일상 실행은 설치된 앱 열기다.
- Go bootstrap은 `~/.local/bin/hmux` CLI를 설치한다. 개인 셸 함수에 의해
  이름이 덮일 수 있으므로 문서에서는 정확한 경로를 사용한다.
- DMZ 릴리스는 크기·SHA-256·platform/role·protocol·Ed25519 서명을 검증한다.
- 서명 개인키는 신뢰하는 빌드 호스트에 두고 DMZ에는 공개 산출물만 둔다.
- 앱 archive 경로·타입, bundle 정보, CPU, code signature를 검증하고
  같은 볼륨에서 원자적으로 교체하며 이전 버전을 복구 가능하게 둔다.
- 개인용 ad-hoc 서명을 Developer ID/notarization 완료로 표현하지 않는다.
- 기존 사용자 설정은 timestamp 백업과 managed include/block으로 변경한다.

## 보안 경계

1. 실제 주소, 사용자명, 개인키 경로, 비밀을 저장소에 넣지 않는다.
2. 원격 실행은 allowlist와 `exec.Command` 인자 배열을 쓴다.
   OpenSSH remote-shell 인자는 별도로 검증·보호한다.
3. host-key 검증을 끄거나 `UserKnownHostsFile /dev/null`을 쓰지 않는다.
   초기 host key는 기존 신뢰 경로로 확인한다.
4. agent forwarding을 활성화하지 않는다.
5. Termius DB·Vault·LevelDB·SQLite·private app storage를 읽거나 수정하지 않는다.
6. sshd·방화벽·NAT 변경은 기본 범위 밖이다. 필요할 때 구체적인 diff와
   잠금 위험을 제시하고 실제 적용 전에 승인받는다.
7. macOS launchd 작업이나 상시 daemon을 도입하지 않는다.
8. 검증하지 않은 다운로드 스크립트를 파이프로 셸에 실행하지 않는다.
9. 원본 tmux에 UI·환경변수·alias 데이터를 설치하지 않는다.
   구버전 정리는 별도의 백업·검증된 migration 도구로 수행한다.

## 모바일·호환 CLI

Termius에는 DMZ jump와 Home Sessions를 지원되는 UI로 구성한다.
Home startup은 `exec ~/.local/bin/hmux-agent select --mobile`이다.
Vault 동기화와 물리 iPhone 검증은 실제 확인한 수준까지만 보고한다.

일반 CLI attach는 기본 handoff, `--shared`는 동시 접속이다.
Home tmux 내부에서는 switch-client를 사용한다. 호환 프레임은 별도
disposable UI 서버와 shared attach를 사용한다.
키와 동작은 `docs/CLI_COMPATIBILITY.md`에서 설명한다.

## 검증·문서

단위, race, vet, shell 정적 검사, isolated integration, Swift smoke를
실행한다. 네이티브 변경은 전체 앱 빌드로 SwiftUI/Ghostty 연결까지 확인한다.

기존 tmux 세션을 attach·rename·detach·kill하는 테스트는 금지한다.
live test는 생성한 `hmux-e2e-*` 이름과 격리된 socket만 사용하고 자신이
생성한 자원만 정리한다. 실제 원격·모바일 시험은 별도의 작업 범위다.

결과에는 실행 명령, 통과·실패·skip과 환경 제약을 구분한다.
현행 제품·사용법·구현·검증 문서를 분리하고 중복 규칙을 줄인다.
오래된 검증과 미구현 제안은 archive의 비권위 기록이며 현행 지침이 아니다.
